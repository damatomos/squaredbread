import React from 'react';

import styles from './Menu.module.css';

function Menu() {
  return (
    <section className={`page ${styles.wrapper}`}>
      Menu
    </section>
  );
}

export default Menu;